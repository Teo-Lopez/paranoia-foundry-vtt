// TODO: generalize this loop
// export const localizeName = (context, path) => {
//   for (let [k, v] of Object.entries(context['data.attributes'])) {
//     v.label = game.i18n.localize(CONFIG.BOILERPLATE.attributes[k]) ?? k
//   }
// }
